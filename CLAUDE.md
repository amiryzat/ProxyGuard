# ProxyGuard

## Claude Code Startup

- [[START_SESSION]]
## Documentation

Detailed documentation is located in the `docs/` folder.

- [[project-overview]]
- [[architecture]]
- [[roadmap]]
- [[decisions]]
- [[testing]]
- [[bugs]]
- [[report]]


## Project Overview
ProxyGuard is a liveness aware face recognition system for proxy attendance detection. It is being built for a CSC649 (Special Topics in Computer Science) final project proposal at UiTM. The core problem it solves: normal face recognition attendance systems can be fooled when one student checks in on behalf of an absent classmate using a photo, video, or replayed face data. ProxyGuard adds multiple liveness checks on top of face recognition to confirm the person checking in is physically present and actively responding, not a static image or recording.

## Group Members
1. Muhammad Amir Izzat bin Rosdi (2025394889)
2. Muhammad Adli Fadhlan bin Azame (2025180775)
3. Muhammad Izzuddin bin Izad Emi (2025181293)

## Tech Stack
- Python 3.10+
- OpenCV (`opencv-python`) for video capture and face detection
- `face_recognition` (built on dlib) for face encoding and matching
- MediaPipe Face Mesh for blink detection and head pose tracking
- Speech to text: decided — Google Speech Recognition API via the `SpeechRecognition` + `PyAudio` packages (needs internet). Note for Apple Silicon dev machines: `SpeechRecognition`'s bundled `flac-mac` binary is Intel-only and fails with `OSError: Bad CPU type in executable`; fix is `brew install flac` so a native arm64 `flac` is on `PATH` (it's preferred over the bundled binary automatically).
- Flask for the lecturer-facing dashboard (`dashboard/`, built — see `docs/architecture.md`)
- Pandas for attendance logging (CSV based for now)

## Core System Design

### 1. Face Recognition (implemented — `src/face_recognizer.py`)
- Registered students each have one to three frontal reference photos stored in `data/known_faces/` (filenames may have a trailing `_N` for multiple photos of the same person, e.g. `NAME_1.JPG`; the trailing number is stripped when deriving the display name).
- Photos are loaded through PIL with EXIF-based orientation correction before encoding, since phone photos often store rotation as metadata rather than rotating the pixels.
- Each photo is converted into a 128 dimensional face encoding using `face_recognition`.
- Encodings are compared against a live detected face using distance matching (default tolerance 0.45, tightened over two steps: from the originally planned 0.6 to 0.5, then to 0.45 after a false-match incident involving Izzuddin, to reduce false-accept risk between similar-looking students).
- A minimum margin check (default 0.1, raised from 0.05 in the same tightening) between the best and second-best match distances is used to reject ambiguous matches instead of guessing between two close candidates. Tradeoff: with few reference photos per student, more legitimate matches may fall to "Unknown" — the intended direction to err for attendance integrity, since a rejected student can retry but a false accept is a proxy slipping through.
- The standalone test loop (`if __name__ == "__main__"`) downscales frames to 0.25x and only runs recognition every 3rd frame for smoother webcam playback; detection boxes are scaled back up for display.
- `locate_faces(frame)` is a second, cheaper entry point that returns only face bounding boxes (no encoding/comparison). `main.py` uses it to keep the on-screen box tracking a face after identity is already locked in, without repeating the more expensive encoding + distance comparison every frame.
- No side angle photos are needed for recognition. Side angle data is not stored anywhere.

### 2. Liveness Detection (multi-factor, this is the differentiator of the project) — `src/liveness_check.py` (+ `src/voice_challenge.py`, built but removed from the live flow)
Two checks currently run in the live flow (blink + head movement). A third, the voice challenge, is fully implemented as a standalone module but has been **removed from the live check-in loop** at the current proposal stage — see subsection 3 below, the flow note, and `docs/decisions.md` Decision 7. Together the active checks confirm a real, present, responsive person:

1. **Blink detection** (continuous, no prompt needed) — implemented
   Runs in the background the whole time a face is being checked. `BlinkDetector` uses MediaPipe Face Mesh eye landmarks to calculate eye aspect ratio (EAR) per eye and averages both eyes. A blink is counted when EAR drops below `EAR_THRESHOLD` (0.21) for at least `CONSECUTIVE_FRAMES` (2) frames and then recovers. A static photo or frozen video frame will not blink.

2. **Randomized head movement challenge** (active, prompted) — implemented
   `HeadMovementChallenge` picks a random direction from `left`, `right`, `up` (`CHALLENGE_DIRECTIONS`) and gives a 10 second window (`CHALLENGE_DURATION_SECONDS`) to match it. Direction is derived from MediaPipe Face Mesh landmarks (nose tip, left/right eye corners, chin) via normalized horizontal/vertical offset of the nose from face center (`get_head_pose`); the same offsets can also resolve to `down` or `center`, though `down` is not currently one of the challenge's random targets. Randomized per attempt so a pre-recorded video cannot reliably fake it. `main.py` additionally requires a blink to occur during the same window before counting the direction match as a pass, since a tilted static photo can mimic head movement but cannot blink.

3. **Randomized voice/word challenge** (active, prompted) — module implemented (`src/voice_challenge.py`), **NOT currently wired into the live flow** (removed from `main.py`; see subsection 3 and `docs/decisions.md` Decision 7). The module is kept intact and unimported so it can be reintegrated later. As built: `generate_challenge_number()` produces a random 3-digit number. `listen_for_number()` records via the mic (`speech_recognition` + `PyAudio`, 5s timeout / 5s phrase limit) and transcribes with the Google Speech Recognition API; `extract_digits()` converts spoken digit-words ("four", "two"...) or literal digits into a digit string for comparison. `get_digit_matches()` compares the expected number against the transcript position-by-position, returning a per-digit correct/incorrect list (intended for coloring each digit green or red on screen). It checks that a correct, timely verbal response occurred; it does NOT identify who is speaking and no voice biometric data is stored.

### 3. Combined Check-in Flow & Anti-Proxy Safeguards — `src/main.py` (`CheckinSession`), implemented
All per-frame recognition, liveness, logging, and snapshot logic lives in a single class, `CheckinSession` (`src/main.py`), driven through one method, `process_frame(frame) -> annotated_frame`. This was extracted from an earlier inline loop specifically so the desktop entry point (the `if __name__ == "__main__"` block, still the terminal-prompt-driven OpenCV window) and the student web app (`checkin_app/`, Phase 2b — see section 7) can **share the exact same engine**: both construct a `CheckinSession` and feed it frames, and only differ in how frames are captured (OpenCV camera read vs. `cv2.VideoCapture` inside a Flask MJPEG generator) and displayed (`cv2.imshow` vs. a browser `<img>`). A fix to detection/liveness/logging behavior in `CheckinSession` therefore applies to both entry points automatically — it does not need to be duplicated. `CheckinSession.reset()` starts a fresh attempt (desktop `n` key / web "New check-in" button, gated on `can_reset()`, true only in a terminal phase); `close()` releases the per-session mediapipe `FaceMesh`. A `restart_hint` constructor parameter controls the on-frame "Press 'n' to run a new check" text — the desktop entry point uses the default text, `checkin_app` passes `None` since it has a button instead.

**The voice challenge is no longer part of this flow** (removed to prioritize flow reliability at the proposal stage — see `docs/decisions.md` Decision 7); the `countdown`, `listening`, and `voice_result` phases and the mid-challenge identity re-verification described in earlier revisions have been removed.

Current state machine:

`liveness_success` → `confirmed`, or `liveness_success` → `not_recognized`, or `liveness_failed`, or (bypassing `liveness_success` entirely) `duplicate_checkin`. `confirmed`, `not_recognized`, `liveness_failed`, and `duplicate_checkin` are terminal (wait for a reset via `can_reset()`/`reset()`); `liveness_success` advances automatically on a timer. `identity_mismatch` is retained as a terminal state in the code but is **currently unreachable**, since it was only triggered during the (now removed) voice listening phase; it is kept so the flow can be reintegrated later.

- **Duplicate check-in bypass (`_check_duplicate_checkin`)**: once the recognized identity is stable pre-lock (set and held by the identity-continuity guard below — exactly one face, a real registered name, continuity intact), the challenge checks whether that student already has a successful check-in for the current `session_id`, reusing `attendance_logger.has_success_this_session()` rather than re-scanning the CSV in `main.py`. Looked up at most once per distinct stable identity per attempt (`duplicate_checked_name`), so it doesn't re-read the CSV on every processed frame while a non-duplicate student's challenge continues. A match locks the attempt directly into the `duplicate_checkin` terminal state — the liveness challenge is never run to completion for a student already present, and `liveness_success`/"ATTENDANCE CONFIRMED" are never shown for that attempt. The on-frame/status banner reads `"ALREADY CHECKED IN: {name}"`. Logged directly as `result=failed`, `reason=duplicate check-in this session` (the same `DUPLICATE_REASON` constant `attendance_logger.py` and `pattern_flagger.py` already use), rather than passing `result=success` and relying on `log_attendance()`'s own downgrade check to re-derive it — this attempt was never a "confirmed" success to begin with. See `docs/decisions.md` Decision 12, `docs/bugs.md` Bug 12.

- **Single-person enforcement**: the challenge only runs with exactly one face in frame (mediapipe Face Mesh is configured `max_num_faces=1`, so a second face in frame could let one person perform the challenge while a different, possibly registered, face is also present). While more than one face is detected, the challenge pauses — no mediapipe processing, no advancing, no timing out, timer pinned to full time — and an on-screen/status warning ("Only one person allowed in frame, please ensure you are alone during check-in") is shown until only one face remains. See `docs/decisions.md` Decision 10, `docs/bugs.md` Bug 5.
- **Identity-liveness continuity guard (anti-proxy face swap)**: recognizing a face once is **not** sufficient to bind that identity to the rest of the attempt. Once `identified_name` is set, a later single-face recognition check only keeps it bound if the recognized name still matches **and** the face's position stays close to the last confirmed position (a simple center-distance check against a 35%-of-frame-width threshold — not real object tracking). Up to 2 consecutive mismatched checks are tolerated (the same slack the anti-flicker fix below relies on); the 3rd consecutive mismatch clears `identified_name`/`last_known_name`/the tracked position and restarts the challenge from scratch, with a brief "Face changed or lost, please keep the same face in frame" message. The liveness `"passed"` branch additionally only confirms `identified_name` when there is **zero** current mismatch streak — not just "was recognized at some point this attempt" — closing a race window where a pass landing during the tolerance grace period could otherwise still confirm a stale identity. This closes a real bypass: get recognized once, then swap in an unregistered face or another person's photo, and without this guard the challenge could still complete under the first identity. See `docs/decisions.md` Decision 11, `docs/bugs.md` Bug 9.
- **Pre-lock identity tracking (`identified_name`)**: while the head-movement/blink challenge is still in progress (not yet locked), `identified_name` only updates when a processed frame produces a real match (`last_names[0] != "Unknown"`) from a single-face frame, subject to the continuity guard above once an identity is already locked in for the attempt. This matters because turning left/right/up during the challenge routinely causes a stray frame or two where `face_recognition` can't match the off-angle face — without this guard, that single bad frame would overwrite `identified_name` with "Unknown" right as the challenge passes, causing a real registered student to be logged as `not_recognized` purely from momentary angle-induced misdetection during their own head turn.
- **`last_known_name`** mirrors `identified_name`'s protection but for the on-screen box label and the snapshot/log identity specifically; it updates from the same continuity-gated trigger. It is deliberately left uncleared once the challenge locks (so the label/snapshot/log keep showing the last confident identity through a pass or fail), but the drawn label itself is gated on `check_locked` so a locked box never shows a *previous* student's name on a newly-arrived person (`docs/bugs.md` Bug 3).
- **On-screen box color coding**: each detected face's bounding box (and label, pre-lock) is drawn green when recognized, red when unrecognized/`"Unknown"` — visible in both the desktop window and the `checkin_app` browser stream.
- **`liveness_success`** (2s, `LIVENESS_MESSAGE_SECONDS`): shows "LIVENESS DETECTION SUCCESSFUL" bottom-center, then routes to `confirmed` if a face was recognized, or `not_recognized` if not.
- **`confirmed`**: a recognized face that passed the blink + head-movement liveness challenge is confirmed directly (shows "ATTENDANCE CONFIRMED: {name}"). Terminal.
- Bounding box tracking during any locked phase uses `locate_faces()` (position only) rather than full recognition.
- **Logged identity on a terminal outcome**: `confirmed_name` on a pass; on any failed outcome, falls back to `last_known_name` (the recognized identity, if one was established and remained continuous this attempt) before falling back to `"Unknown"` — so a recognized student who fails liveness is logged and shown under their real name, not `"Unknown"` (`docs/bugs.md` Bug 8). The verification snapshot filename uses the same fallback and the reason **actually written** to the CSV (post duplicate-downgrade, `docs/bugs.md` Bug 4), so the two stay consistent.

### 4. Attendance Logging (implemented — `src/attendance_logger.py`)
- CSV schema is `name`, `date`, `time`, `session_id`, `result`, `reason` — split into a `result` column (always `"success"` or `"failed"`) and a separate `reason` column (short human-readable string: `"confirmed"`, `"liveness timeout"`, `"face not recognized"`, or `"duplicate check-in this session"`; plus `"identity mismatch"`, which the code can still write but is currently unreachable now that the voice listening phase is removed), replacing an earlier single combined `status` column (e.g. `"liveness_failed"`, `"voice_failed"`) that mixed outcome and reason together.
- `log_attendance(name, result, reason, session_id, log_path=DEFAULT_LOG_PATH)` appends one row, creating the file with a header row the first time it's called if the file doesn't exist yet (or exists but is empty). If an existing file has a different/older header (schema mismatch), `_ensure_log_file` archives it to `attendance_legacy.csv` instead of overwriting or mixing row formats, then starts a fresh file with the current header — this ran once automatically when the schema changed from the old single-`status` format.
- **Duplicate detection is scoped to a session (`session_id`), not a calendar date.** `has_success_this_session(name, session_id, log_path)` only compares rows with a matching `session_id`; a new `"success"` result is downgraded to `"failed"` / `"duplicate check-in this session"` if that session already has one. This was changed from date-based dedup because a single room/date can host multiple separate class sessions — a date check would wrongly treat two different classes as duplicates of each other, or block a legitimate re-check-in in a later class the same day.
- `main.py` generates `session_id` exactly once per run, near the top of its `if __name__ == "__main__"` entry point — restarting `main.py` naturally starts a fresh session, so duplicate check-ins from a previous run no longer count against the new one. As of Phase 1 of a session-restructuring feature, `session_id` is no longer a bare timestamp: before the camera loop starts, `main.py` calls `prompt_session_setup()` to ask the lecturer (in the terminal) to pick a class subject from a small predefined list and a week number (1–14), then builds `session_id` with `build_session_id(subject, week_number)`. Both the predefined subject list (`CLASS_SUBJECTS`) and `build_session_id()` live in a new standalone module, `src/class_config.py` (dependency-free, matching the module-independence convention), not in `main.py` itself, so other entry points can reuse the exact same format without importing `cv2`/`mediapipe`. Format: `YYYY-MM-DD_HHMM_<CODE>_Week<N>`, e.g. `2026-07-07_0900_CSC649_Week3` — `<CODE>` is the short code portion of the selected subject (e.g. `CSC649` from `"CSC649 - Special Topics in Computer Science"`), kept space-free since it also feeds into snapshot filenames. See `docs/decisions.md` Decision 8. `attendance_logger.py` and `pattern_flagger.py` were confirmed to treat `session_id` as an opaque string (string equality / grouping only, never parsed), so neither needed changes for this new format.
- Every terminal outcome gets logged, not just successful ones: `CheckinSession.process_frame` calls `log_attendance` exactly once per check-in attempt, the moment `flow_phase` reaches a terminal state (`confirmed`, `not_recognized`, `liveness_failed`, `duplicate_checkin`, or the currently-unreachable `identity_mismatch`), guarded by an `outcome_logged` flag so it isn't re-logged every frame while the terminal message is on screen. The student name logged is `confirmed_name` on a pass; on a failed outcome it falls back to `last_known_name` (the recognized identity, if one was confidently established and remained continuous during the attempt) before falling back to `"Unknown"` — so a recognized student who fails liveness is logged under their real name rather than `"Unknown"` (see Core System Design §3, and `docs/bugs.md` Bug 8). `identified_name`/`confirmed_name` are only ever truthy when `last_known_name` is too (they update from the same trigger), so this fallback never changes what gets logged on a successful confirmation.
- **Verification snapshot per outcome**: at the same terminal moment (inside the same `outcome_logged` guard), `CheckinSession` also saves a snapshot of the webcam frame to `logs/snapshots/` (auto-created), for successes as well as failures. It saves a pre-overlay `clean_frame` copy taken right after the flip — before any boxes/text/challenge number are drawn — so the face is unobstructed. Filename is `{session_id}_{name}_{reason}_{HHMMSS}.jpg`, where `name` is `last_known_name` (or `unknown`, slugified) — the same identity used for the CSV row above — and `reason` is the reason **actually written** to the CSV by `log_attendance` (which may differ from the pre-call reason if a duplicate success was downgraded; see `docs/bugs.md` Bug 4), so the snapshot filename and the CSV row always agree for the same attempt. This is a manual lecturer-review aid only (never used to auto-accept/reject), motivated by the Izzuddin false-match incident: a check-in can pass the liveness checks and still be a photo of someone else, so the actual image needs to be reviewable after the fact.
- The module has no dependency on any other `src/` module (only `csv`, `os`, `datetime`), matching the project's independent-module convention, and has a manual smoke-test block under `if __name__ == "__main__"` that logs sample rows under two different fake `session_id`s to demonstrate both the duplicate downgrade (same session) and the non-duplicate case (different session, same student).

### 5. Proxy/Suspicious Pattern Flagging (implemented — `src/pattern_flagger.py`)
Reads `logs/attendance.csv` and flags three patterns for lecturer review (never auto-rejects):
- **Repeated failures** (`find_repeated_failures`): any registered (non-`"Unknown"`) name with at least `REPEATED_FAILURE_THRESHOLD` (3) `"failed"` rows across the whole log, regardless of session — a signal about that person, not scoped to one class.
- **Duplicate attempts** (`find_duplicate_attempts`): groups rows with reason `"duplicate check-in this session"` by `(name, session_id)`, surfacing who tried to check in more than once successfully within the same session.
- **Clustered unrecognized attempts** (`find_unrecognized_clusters`): flags a `session_id` where `"face not recognized"` rows occur at least `UNRECOGNIZED_CLUSTER_THRESHOLD` (2) times within `UNRECOGNIZED_CLUSTER_WINDOW_MINUTES` (5) of each other. Grouped by session rather than name, since these rows are always logged under `"Unknown"`.
- `flag_patterns(log_path)` returns all three as a dict; `print_report(report)` renders it for the CLI. The module defines its own `DEFAULT_LOG_PATH` rather than importing it from `attendance_logger.py`, keeping it independently importable per project convention. Its `if __name__ == "__main__"` block writes a small sample CSV (in the system temp dir, not the real log) with two fake `session_id`s and runs all three checks against it.

### 6. Session Setup Config (implemented — `src/class_config.py`)
Standalone, dependency-free module (only `datetime`) introduced in Phase 1 of the session-restructuring feature described in section 4 above. Holds `CLASS_SUBJECTS` (the predefined, easily-editable list of `"<CODE> - <Full subject name>"` entries a lecturer can pick from), `WEEK_MIN`/`WEEK_MAX` (1–14), `subject_code(subject)` (splits out the short code before `" - "`), and `build_session_id(subject, week_number, now=None)` (the single source of truth for the `session_id` format — see section 4). Kept dependency-free and outside `main.py` specifically so a second entry point could import the same subject list and id-building logic without pulling in `cv2`/`mediapipe`; this is exactly what `checkin_app/` (section 7 below) does.

### 7. Student Check-in Web App (implemented — `checkin_app/`)
A new, standalone Flask app — separate from the lecturer `dashboard/` app — living in its own top-level `checkin_app/` folder, matching the project's module-independence convention. **Phase 2a** is the session setup page; **Phase 2b** adds the live check-in flow, streamed into the browser. It imports `CLASS_SUBJECTS`, `WEEK_MIN`, `WEEK_MAX`, and `build_session_id()` directly from `src/class_config.py` (added to `sys.path` the same way `dashboard/app.py` already does) rather than duplicating the Phase 1 subject list or the `session_id` format, and (Phase 2b) imports `CheckinSession` directly from `src/main.py`.
- `GET /` renders `templates/setup.html`: a class-subject dropdown and a week-number dropdown (1–14), plus a "Start Session" button.
- `POST /start` validates the submitted subject/week against the known Phase 1 values (never trusting raw form input — an invalid or tampered submission re-renders the setup form with an error instead of building a malformed id), builds `session_id` via `build_session_id()`, starts a `CheckinSession` for it, and redirects to the live check-in page (`templates/checkin.html`). `templates/confirm.html` is a Phase 2a leftover, no longer used by `/start`.
- **Live check-in (Phase 2b)**: `GET /checkin` renders the session header (class/week/`session_id`) plus an `<img>` pointed at `GET /video_feed`, an MJPEG stream (`multipart/x-mixed-replace`) of frames captured server-side and run through `CheckinSession.process_frame(...)` — the browser only displays frames, it never accesses the camera directly. `GET /status` (polled by the page) reports the current `flow_phase`/banner text and whether a reset is allowed; `POST /reset` is the web analog of the desktop `n` key ("New check-in" button, enabled only at a terminal outcome). One physical webcam ⇒ one active station: the camera and `CheckinSession` (and its one mediapipe `FaceMesh`) are opened once and reused across resets, not recreated per attempt.
- **No detection/liveness/logging logic is duplicated here** — the app drives the exact same `CheckinSession` the desktop flow uses (see Core System Design §3), so every fix there (multi-face blocking, the identity-continuity guard, correct failed-liveness name logging, etc.) applies to the web app automatically.
- **Streaming performance** (tuned for a fanless CPU-only machine, e.g. MacBook Air; see `docs/bugs.md` Bug 6/Bug 7): capture and stream at 1280×720 with JPEG quality 80 — processing resolution is decoupled from display resolution, since `CheckinSession.process_frame` already downscales internally for recognition (0.25x) and mediapipe (0.5x) and draws results back onto the full-size frame, so a clear capture size doesn't cost extra recognition/mediapipe time, only a larger JPEG encode. The generator loop is capped at ~20 FPS, and a `_stream_generation` counter ensures only the newest `/video_feed` connection drives the camera (older ones from a reload/reconnect exit their loop), preventing thread/handle accumulation over a long session.
- **Skeleton loading UI**: submitting the setup form immediately hides the picker and shows a full check-in-page skeleton (`templates/_checkin_skeleton.html`, shared with `checkin.html`) with a shimmering placeholder for every real section (heading, session card, status card, camera frame, action buttons) and a "Preparing check-in session…" message, then submits the form programmatically after one paint — client-side only, no change to the blocking `POST /start`. On `checkin.html` itself the same skeleton is shown by default and hidden only once the MJPEG `<img>` has decoded a genuine first frame (its `load` event) **and** `/status` reports the station active, never on a fixed timeout; a 30s timeout instead surfaces a distinct failure state with a "Try again" action. Note: the `<select>` elements are deliberately never disabled during this transition (only the submit button is) — a disabled form control is excluded from the submitted form data, which previously silently stripped `subject`/`week` from the POST (`docs/bugs.md` Bug 11).
- HTML/CSS/JS are separated (`templates/`, `static/css/checkin.css`, `static/js/checkin.js`); `checkin.js` guards each page's wiring by element existence so one file serves both `setup.html` and `checkin.html`.
- Runs on port 5002 (`python checkin_app/app.py`, `threaded=True`, no debug reloader — it would spawn a second process fighting over the webcam, so the process must be restarted manually to pick up code changes), distinct from the dashboard's 5001 and macOS AirPlay's 5000.
- See `docs/decisions.md` Decision 9 and Decision 17, and `docs/architecture.md`'s Student Check-in Web App section for full detail.

### 8. Lecturer Dashboard (implemented — `dashboard/`)
A standalone, read-only Flask app (`dashboard/app.py`, port 5001) that reads `logs/attendance.csv` and `logs/snapshots/` and never writes to either; it reads and writes `logs/reviews.csv` for the review workflow below. HTML/CSS/JS are separated (`templates/`, `static/css/dashboard.css`, `static/js/dashboard.js`), following one shared monochromatic design system with `checkin_app/` (`docs/ui-references/DESIGN.md`).
- **Present / Attempts tabs**: the table is split into two tabs on one page (client-side toggle, no separate routes) — Present shows `result=success` rows for the selected session, Attempts shows `result=failed` rows.
- **Session filter** (dropdown, defaults to most recent session); **Attempts-tab quick filters** (instant client-side chips: All/Successful/Failed/Flagged/Unknown/Duplicate/Liveness Failed, replacing an earlier reason dropdown) and **review-status filters** (All Reviews/Unreviewed/Accepted/Suspicious) apply together, combined with a client-side **search** box (persisted in `localStorage`).
- **Analytics cards** (session-wide totals, success rate, flagged/duplicate/unknown/liveness-failed counts) and a **Reason Breakdown** bar chart, both session-wide regardless of the active tab.
- **Inline verification snapshots** with a **snapshot preview modal**: each row shows a thumbnail of its matched snapshot file; clicking opens a modal with the full-size image and the row's metadata instead of navigating away. Matching (`find_snapshot_for_row()`) rebuilds the snapshot filename prefix from the row's `session_id`/`name`/`reason` (mirroring `build_snapshot_filename()` in `src/main.py`); if that strict match fails (e.g. an older row where the CSV `name` and the snapshot's name segment diverge — see `docs/bugs.md` Bug 8), it falls back to matching on `session_id` + `reason` + the row's own timestamp, ignoring the name segment.
- **Lecturer review workflow**: each Attempts row can be marked Accepted or Suspicious with an optional note (`POST /review`), stored independently in `logs/reviews.csv` (keyed by a hash of the row's own fields) so `logs/attendance.csv` is never modified. A per-session Review Summary (Unreviewed/Accepted/Suspicious) sits above the table; a review action updates the row badge/note/summary/filters/assistant in place, no page reload. See `docs/decisions.md` Decision 15.
- **Smart auto-refresh**: polls `GET /status` and reloads only when a content-derived version token changes (row count + latest row fields + `reviews.csv` size + newest snapshot name — not filesystem `mtime` alone), with explicit no-cache response headers and a `cache: "no-store"` client fetch; a change detected mid-interaction is deferred and applied the instant the interaction ends rather than on the next poll tick or discarded. See `docs/decisions.md` Decision 14, `docs/bugs.md` Bug 10.
- **ProxyGuard Assistant**: a floating, rule-based recommendation panel (no external AI, reachable from both tabs) — a compact session summary (success-rate bar, attention level, main concern, items needing review) followed by up to 5 prioritized, resolution-aware recommendation cards (flagged/suspicious, duplicate, unreviewed, unknown, liveness failures, low success rate, or a normal-session message), each with a "View affected attempts" / "Open next unreviewed attempt" button that reuses the existing filter chips and table rather than a second filtering system. See `docs/decisions.md` Decision 13.
- **CSV export** (`GET /export.csv`) mirrors exactly the currently filtered view (session/tab/search/quick filter/review filter).
- **Flagged row highlighting** reuses `pattern_flagger.flag_patterns()` (no duplicated detection logic); the same per-row flag also feeds the Assistant's rules.
- See `docs/architecture.md`'s Lecturer Dashboard section for full detail.

## Planned Project Structure
```
proxyguard/
├── data/
│   └── known_faces/        # one to three frontal reference photos per student
├── src/
│   ├── face_detector.py    # OpenCV Haar Cascade face detection (done)
│   ├── face_recognizer.py  # face_recognition based identity matching (done)
│   ├── liveness_check.py   # blink detection + head movement challenge (done)
│   ├── voice_challenge.py  # randomized number generation + speech-to-text check (built, but not wired into main.py's live flow)
│   ├── class_config.py     # predefined class subject list, week bounds, build_session_id() (done — Phase 1)
│   ├── attendance_logger.py# logs check-ins to CSV (name/date/time/session_id/result/reason), session-scoped duplicate-check (done)
│   ├── pattern_flagger.py  # reads attendance CSV, flags repeated failures / duplicates / unrecognized clusters (done)
│   ├── main.py             # CheckinSession: shared check-in engine (recognition, liveness, multi-face + identity-continuity + duplicate-before-liveness guards, logging, snapshots) + the desktop entry point that drives it (done)
│   ├── test_face_continuity.py  # minimal assert-based self-check for the identity-continuity guard's geometric threshold (done)
│   ├── test_duplicate_checkin.py # minimal assert-based self-check for the duplicate-before-liveness bypass (done)
│   └── experiment_far_frr.py    # leave-one-out FAR/FRR + streaming-throughput experiment over data/known_faces/, backing docs/report.md Section 9 (done)
├── dashboard/              # standalone Flask lecturer dashboard (done — see docs/architecture.md)
│   ├── app.py              # attendance/review data assembly, filters, analytics, review workflow, ProxyGuard Assistant rules
│   ├── test_assistant.py   # minimal assert-based self-check for the Assistant's rule engine (done)
│   ├── templates/
│   │   ├── index.html              # tabs, filters, table, snapshot/note modals, floating assistant
│   │   └── _assistant_panel.html   # assistant summary + recommendation cards (re-rendered by /assistant_panel)
│   └── static/
│       ├── css/dashboard.css
│       └── js/dashboard.js
├── checkin_app/            # standalone Flask student check-in web app: session picker + live MJPEG check-in (done — see docs/architecture.md)
│   ├── app.py              # session picker -> builds session_id via class_config.build_session_id() -> starts a CheckinSession -> live check-in page (MJPEG stream, status polling, reset, end session)
│   ├── templates/
│   │   ├── setup.html               # session picker + client-side skeleton-loading handoff
│   │   ├── confirm.html             # Phase 2a leftover, no longer used by /start
│   │   ├── checkin.html             # live check-in page: session header, MJPEG <img>, status line, action buttons
│   │   └── _checkin_skeleton.html   # shared skeleton markup used by both setup.html and checkin.html
│   └── static/
│       ├── css/checkin.css
│       └── js/checkin.js
├── logs/
│   ├── attendance.csv
│   ├── reviews.csv         # lecturer review decisions (Accept/Suspicious + note), independent of attendance.csv
│   └── snapshots/          # per-outcome verification snapshots (session_id + name + reason + timestamp .jpg)
├── requirements.txt
└── README.md
```

## Build Status
- [x] Step 1: Environment and folder structure
- [x] Step 2: Face detection (`face_detector.py`) using OpenCV Haar Cascade
- [x] Step 3: Face recognition (`face_recognizer.py`) using `face_recognition` encodings, EXIF orientation correction, and margin-based disambiguation
- [x] Step 4: Liveness detection
  - [x] Blink detection (EAR-based, continuous) — `liveness_check.py`
  - [x] Randomized head movement challenge (left/right/up, 10s window) — `liveness_check.py`
  - [x] Randomized voice/word challenge (Google Speech Recognition API, 3-digit number, per-digit matching) — `voice_challenge.py` (module built, but **removed from the live `main.py` flow** — see Core System Design §2/§3 and `docs/decisions.md` Decision 7)
- [x] Step 4.5 (not in original plan, added during integration): combined check-in flow — `CheckinSession` in `main.py` (see Core System Design section 3). Note: the anti-proxy identity re-verification originally added here only ran during the voice listening phase, which has since been removed; it has since been superseded by the identity-continuity guard below, which runs independently of any voice phase.
- [x] Step 5: Attendance logging system (`attendance_logger.py`) — logs every terminal outcome to `logs/attendance.csv` with a `result`/`reason` split (not a single combined status column), a `session_id` per run, auto-creates/migrates the file's headers, and prevents duplicate successful check-ins scoped to a session (not a calendar date)
- [x] Step 6: Proxy detection / suspicious pattern flagging logic (`pattern_flagger.py`) — reads the attendance log from Step 5 and flags repeated failures per identity, duplicate attempts per session, and clusters of unrecognized-face attempts per session (beyond the real-time safeguards already in `CheckinSession`, which prevent a swap live rather than flagging it after the fact)
- [x] Step 7: Lecturer dashboard (`dashboard/`) — standalone Flask web app with Present/Attempts tabs, session + reason filters, flagged-row highlighting that reuses `pattern_flagger.py` (no duplicated logic), a per-tab summary (total/successful/failed/flagged), and inline verification-snapshot thumbnails per row. Run with `python dashboard/app.py` (port 5001). See `docs/architecture.md` and Core System Design section 8.
- [x] Step 7.5 (not in original plan, Phase 1 of a session-restructuring feature): `session_id` now includes class subject + week context, not just a timestamp — `src/class_config.py` holds the predefined subject list and `build_session_id()`; `src/main.py` prompts the lecturer in the terminal for class + week before generating it. See Core System Design section 6 and `docs/decisions.md` Decision 8.
- [x] Step 7.6 (not in original plan, student check-in web page): `checkin_app/` — standalone Flask app with a session **setup page** (Phase 2a) and a **live check-in page** (Phase 2b) that streams the webcam into the browser as MJPEG, driven by the same shared `CheckinSession` engine as the desktop app — no duplicated recognition/liveness/logging logic. Run with `python checkin_app/app.py` (port 5002). See Core System Design section 7 and `docs/decisions.md` Decision 9.
- [x] Step 7.7 (not in original plan, anti-proxy hardening after live testing): single-person enforcement during the active challenge (`docs/decisions.md` Decision 10) and an identity-liveness continuity guard preventing a recognized identity from transferring to a swapped-in face/photo (`docs/decisions.md` Decision 11) — both in the shared `CheckinSession`, so both entry points enforce them identically. See Core System Design section 3 and `docs/bugs.md` Bug 5/Bug 9.
- [x] Step 7.8 (not in original plan): dashboard redesign onto a separated HTML/CSS/JS architecture (`dashboard/static/`), plus a large batch of new dashboard features — analytics cards, Reason Breakdown, snapshot preview modal, quick filters, client-side search, smart auto-refresh (content-derived version token, no-cache headers), CSV export, a lecturer review workflow (`logs/reviews.csv`, Accept/Suspicious + note), and the rule-based ProxyGuard Assistant recommendation panel. See Core System Design section 8, `docs/architecture.md`, and `docs/decisions.md` Decisions 13–16.
- [x] Step 7.9 (not in original plan): `checkin_app/` client-side skeleton loading state for session startup (session picker → live check-in handoff), plus HTML/CSS/JS separation matching the dashboard. See Core System Design section 7, `docs/decisions.md` Decision 17, `docs/bugs.md` Bug 11.
- [x] Step 7.10 (not in original plan, anti-proxy hardening): duplicate check-in is now detected as soon as the recognized identity is stable, bypassing the liveness challenge entirely (`duplicate_checkin` terminal state) instead of only being caught afterward by `attendance_logger.py`'s downgrade. See Core System Design section 3, `docs/decisions.md` Decision 12, `docs/bugs.md` Bug 12.
- [ ] Step 8: Testing and refinement (ongoing — see `docs/testing.md`)

## Working Conventions
- Each module in `src/` should stay independent and importable on its own (`face_detector.py`, `face_recognizer.py`, `liveness_check.py`, `voice_challenge.py`, `class_config.py`, `attendance_logger.py`, `pattern_flagger.py`), combined together in `main.py`. This is intentional since three group members are working on separate modules to avoid merge conflicts.
- Keep functions small and testable in isolation (for example, a function should detect faces, a separate function should draw them, rather than combining both).
- Prefer clear, well-commented code over premature optimization since this is an academic proposal build, not a production system.
- No raw voice data or side-face images should ever be stored. What persists: frontal face images/encodings, attendance logs, and per-outcome verification snapshots (`logs/snapshots/`, frontal webcam frames used only for manual lecturer review).
- When adding new liveness checks or modifying thresholds (like the 0.45 face match tolerance or blink ratio threshold), note the reasoning in a comment since these values affect both accuracy and the project's write-up later.

## Known Open Decisions
- ~~Speech to text engine not finalized~~ — resolved: Google Speech Recognition API via `SpeechRecognition`/`PyAudio` (needs internet). Whisper was not adopted. Note: the voice challenge that used this has since been **removed from the live check-in flow** (module retained for possible reintegration — see `docs/decisions.md` Decision 7).
- Attendance storage implemented as CSV (`attendance_logger.py`); may still move to SQLite if querying needs grow, but CSV is sufficient for now.
- Dashboard (Flask) is fully built (`dashboard/`, Step 7 + Phase A–F additions — see Core System Design section 8 and `docs/architecture.md`): analytics, snapshot modal, filters, smart refresh, a lecturer review workflow, and the rule-based ProxyGuard Assistant all work end to end. Liveness checks, attendance logging (including the pre-liveness duplicate check), pattern flagging, and the lecturer dashboard all work end to end; only testing/refinement (Step 8) remains of the planned steps.
- Known limitation, not yet addressed: a live video call of the real student held up to the camera can genuinely blink and turn its head on command, so the current liveness checks can't distinguish "real webcam capture" from "webcam capturing a screen showing a live call." This is distinct from the swapped-photo/face-swap loophole closed by the identity-continuity guard (Decision 11) — that guard defeats swapping to a *different* face mid-attempt, but a live call of the *same* recognized person genuinely performing the actions isn't caught by continuity or liveness checks alone. Would need a separate countermeasure (e.g. screen/moiré detection) — out of scope for now.
- Student check-in web app (`checkin_app/`) implements the session picker (with a client-side skeleton loading handoff — Decision 17) and the live check-in flow (camera streamed into the browser as MJPEG, driven by the shared `CheckinSession`) — see Core System Design section 7. Both `src/main.py`'s webcam loop and `checkin_app/` are real check-in entry points now, sharing one engine, including the duplicate-before-liveness bypass (Decision 12).


## Documentation Maintenance

When completing a task:

- Update `docs/roadmap.md` if project progress has changed.
- Update `docs/decisions.md` if an important technical decision was made.
- Update `docs/testing.md` if new testing was performed.
- Update other documentation if it becomes outdated.
- Never leave the documentation inconsistent with the codebase.
